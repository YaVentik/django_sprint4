services:
  db:
    image: postgres:16
    container_name: blogicum_postgres
    env_file:
      - .env.docker
    ports:
      - "5434:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  postgres_data: